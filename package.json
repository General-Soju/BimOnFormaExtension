{
  "name": "bimon-forma-extension",
  "version": "0.1.0",
  "description": "BimOn AI — Forma embedded extension bridging Claude MCP to the Forma SDK over a local WebSocket",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "forma-embedded-view-sdk": "^0.93.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
